{{- if and .Values.enabled .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "lifecycle-service.serviceAccountName" . }}
  labels:
    {{- include "lifecycle-service.labels" . | nindent 4 }}
  annotations:
    {{- include "lifecycle-service.irsaAnnotation" . | nindent 4 }}
    {{- with .Values.serviceAccount.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
automountServiceAccountToken: true
{{- end }}
